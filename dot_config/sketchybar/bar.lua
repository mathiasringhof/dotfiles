local colors = require("colors")

sbar.bar({
	height = 32,
	color = colors.transparent,
	position = "top",
	topmost = "off",
	sticky = "on",
	padding_left = 24,
	padding_right = 24,
	corner_radius = 0,
	blur_radius = 32,
	notch_width = 170,
})
